import {Action, ActionReducer} from "@ngrx/store";
import {PosStepStateRecord} from "../step.state";

export const orderListAddPaymentReducer: ActionReducer<PosStepStateRecord> = (state: PosStepStateRecord, action: Action) => {
  
  return state;
};
